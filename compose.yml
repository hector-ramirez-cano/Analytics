services:
  postgres_db:
    image: postgres:13.22-trixie
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: AnalyticsDB
      TZ: "America/Mexico_City"
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
      - ./backend/initdb:/docker-entrypoint-initdb.d

  influx_db:
    image: influxdb:2.7
    container_name: influx_db
    restart: always
    ports:
      - "8086:8086"
    volumes:
      - ./influxdb2:/var/lib/influxdb2
    environment:
      - TZ=America/Mexico_City

  rsyslog:
    image: rsyslog/rsyslog:latest
    container_name: rsyslog
    ports:
      - "514:514/udp"
      - "514:514/tcp"
    volumes:
      - ./rsyslog.conf:/etc/rsyslog.conf:ro
    networks:
      - syslognet

  # backend_aegis:
  #   image: aegis-backend
  #   container_name: aegis-backend
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     ANSIBLE_PRIVATE_KEY_FILE: /root/.ssh/id_ed25519
  #   volumes:
  #     - ./backend_aegis/identity/id_ed25519:/root/.ssh/id_ed25519:ro
  #     - ./backend_aegis/identity/influx_db.json:/analytics/identity/influx_db.json:ro
  #     - ./backend_aegis/identity/postgres_db.json:/analytics/identity/postgres_db.json:ro
  #     - ./backend_aegis/identity/telegram.json:/analytics/identity/telegram.json:ro
  #     - ./backend_aegis/config.json:/analytics/config.json:ro
  #   stdin_open: true
  #   tty: true
  #   restart: always
  #   depends_on:
  #     - postgres_db
  #     - influx_db
  #     - rsyslog

networks:
  syslognet:
    driver: bridge

volumes:
  postgres_data:
